# 产品更新日志 — Lulu's Daily Mic

## v1.0 — 初始版本发布

### 迭代基础信息
- **更新时间**：2026-05-18
- **版本号**：v1.0
- **迭代类型**：新增

### 本次改动核心功能

1. **口语脚本生成**：支持从 YouTube/Bilibili/网页/主题四种输入源生成约2分钟英文口播脚本。脚本包含固定打卡开篇（Hi guys, it's Lulu. Day X of my daily English practice...）和固定结尾（Thank you for listening, see you tomorrow.），附带完整中文对照翻译。脚本风格严格约束为日常简单词汇+短句+客观转述视角，适配小红书短视频口播节奏。

2. **AI PM 学习材料推送**：支持早晚两次（10:00/22:00）自动策展推送 AI 产品经理学习资源，每次3-5条精选内容，附带中文推荐描述和分类标签。

3. **Web UI 网页端**：基于 Streamlit 的三页面界面，温暖学术风格设计。脚本生成页（输入→生成→双语展示→下载）、脚本历史浏览页（搜索→展开→下载/删除）、学习材料浏览页（最新推送横幅→材料列表→历史记录）。

4. **CLI 命令行**：基于 Rich 的交互式终端界面，支持命令模式和自然语言输入，适配定时任务触发。

5. **macOS 原生通知**：定时推送触发后弹出系统通知，引导用户打开网页查看完整内容。

### 本次使用 AI 技术

- **LLM**：DeepSeek-chat（deepseek-chat），通过 OpenAI 兼容接口调用，JSON mode 强制结构化输出
- **Prompt Engineering**：双重约束策略——固定开篇/结尾在 prompt 中重复三次强调（正文+格式说明+结尾提醒），词汇限制使用「违反即失败」的强语气
- **内容抓取**：youtube-transcript-api（免费免 API key）、Bilibili 公开 API、trafilatura 网页正文提取
- **调度机制**：Claude Code CronCreate 定时触发 + macOS osascript 原生通知
- **存储方案**：纯 JSON 文件存储，零数据库依赖，索引文件 + 实体文件分离
- **前端框架**：Streamlit 1.28 + 自定义 CSS（温暖学术风：奶油色底、Newsreader 衬线字体、琥珀色点缀）

### 开发落地难点

1. **Prompt 约束力不足风险**：LLM 天生有「创造性输出」倾向，容易改写固定开篇结尾、使用更复杂的词汇和句式。解决方案是在 prompt 中用多层强调+惩罚性语言强化约束，并在生成后做自动检测。

2. **内容抓取覆盖率有限**：YouTube 不是所有视频都有字幕，Bilibili 字幕覆盖面更低。技术上采用了四级回退机制（英文字幕→中文字幕翻译→任意可用字幕→标题/简介），确保即使字幕缺失也能生成脚本。

3. **推送触达问题**：纯网页端无法主动触达用户——用户不打开网页就看不到推送。解决方案是引入 macOS 原生通知（osascript display notification）作为桥接层：CronCreate 触发脚本生成 → 保存数据 → 发系统通知（带生动文案引导） → 用户打开网页查看。

4. **产品定位与英语水平匹配**：市面工具要么面向初学者要么面向高级学者，精准匹配「雅思6.5 + AI PM职业场景」是一个空白地带。产品设计上选择以内容题材（AI PM相关）和语言难度（高频2000词+短句）作为差异化定位。

### 解决方案与取舍

**做的选择**：
- ✅ 用 DeepSeek 而非 Claude/GPT：成本优先，日常高频调用场景下性价比是首要考虑
- ✅ 用纯 JSON 文件存储而非 SQLite：零依赖、可手动编辑、面试可直接展示数据结构
- ✅ 用 Streamlit 而非 React/Next.js：开发速度优先，Streamlit 现有成熟模板可复用
- ✅ 用 macOS 通知而非 Telegram Bot：v1.0 先跑通核心流程，减少外部平台依赖
- ✅ 用免费字幕 API 而非 YouTube Data API：免除 API key 管理和费用控制

**放弃的方案**：
- ❌ 不做语音合成（TTS）集成：v1.0 聚焦脚本生成，语音朗读留到后续迭代
- ❌ 不做发音评分：涉及复杂的语音识别和评分算法，过度工程化
- ❌ 不做手机推送：先验证 macOS 桌面端使用场景，手机端后续加 Telegram Bot
- ❌ 不做用户系统/登录：单人使用场景，无需多用户隔离

### 产品复盘思考

本次迭代的核心收获是**「推送触达」问题的解决方式**。起初想直接做 Telegram Bot 解决手机推送，但后来发现对于「坐在电脑前学习」这个场景，macOS 桌面通知已经足够。这个取舍体现了做 AI PM 产品的一个重要原则：**先定义清楚使用场景，再选择技术方案，而不是反过来**。

另一个思考是关于 Prompt 设计中的「惩罚式约束」——用严厉语气（FAILURE IF VIOLATED、COPY EXACTLY）来对抗 LLM 的创造性。这是一种 Prompt Engineering 中的「对抗设计」思路，在需要严格格式输出的场景下有效，但也要注意不触发模型的抵抗行为。后续可以探索用 few-shot 示例替代惩罚式语言。

产品架构上刻意保持了高内聚低耦合，每个 Skill 独立运作，后续加 Telegram Bot 或语音功能只需加新模块，不影响现有流程。

---

## v1.0.1 — 网页文本颜色 bug 修复

### 迭代基础信息
- **更新时间**：2026-05-18
- **版本号**：v1.0.1
- **迭代类型**：修复

### 本次改动核心功能
修复 Web UI 在系统暗色模式下输入文字不可见的 bug：自定义 CSS 仅设置了浅色背景（奶油色/暖白色），但未覆盖 Streamlit 原生的文本颜色。当用户 macOS 处于暗色模式时，Streamlit 自动应用浅色文本，导致输入框、下拉框、展开面板等组件出现「白字白底」问题，用户完全看不清输入内容。

### 本次使用 AI 技术
- **Streamlit 主题配置**：在 `.streamlit/config.toml` 中新增 `[theme]` 配置节，显式声明 `base = "light"` 强制浅色主题，阻断系统暗色模式的自动跟随
- **CSS 全面覆盖**：为 `.stApp`、`.stTextInput`、`.stSelectbox`、`.stMarkdown`、`.stCaption`、`.stExpander` 等关键组件补充 `color` 属性，设置浏览器 autofill 样式覆盖，确保全部 UI 文字使用深色墨水色

### 开发落地难点
Streamlit 的主题系统默认跟随系统设定，而其 CSS 类名和 DOM 结构并非公开 API，需要通过浏览器 DevTools 逐个调试找到准确的 CSS 选择器。不同组件（输入框、下拉框、展开面板）使用的是不同的 BaseWeb 组件树，需要分别定位。

### 解决方案与取舍
- ✅ 采用「双保险」策略：config.toml 层（防止主题跟随系统）+ CSS 层（精确覆盖剩余遗漏组件）
- ✅ 在 `.stApp` 上设置全局 `color` 作为兜底，而非逐个覆盖所有可能的组件
- ❌ 不做暗色模式适配：产品定位为桌面白天学习场景，不需要暗色模式支持

### 产品复盘思考
这是一个典型的「自定义样式覆盖不完全」问题，在 Streamlit 这类高度封装的前端框架中尤其常见。框架默认行为的假设（light theme）和用户系统环境（dark mode）不一致时，就会出现这类视觉 bug。教训是：使用封装框架做自定义 UI 时，必须显式声明基础主题（base theme），再在此基础上做 CSS 覆盖，不能依赖隐式默认值。

---

## v1.0.2 — Quick Topics 报错修复 + YouTube 内容抓取增强

### 迭代基础信息
- **更新时间**：2026-05-19
- **版本号**：v1.0.2
- **迭代类型**：修复 + 优化

### 本次改动核心功能

1. **Quick Topics 点击报错修复**：修复点击快捷主题按钮时的 `StreamlitAPIException: st.session_state.script_input cannot be modified after the widget...` 错误。根因是 Streamlit 控件执行顺序问题 — text_input 控件先于 quick topics 按钮渲染，按钮尝试修改已被控件「占用」的 session_state key。

2. **YouTube 内容抓取失败不再胡乱生成**：当 YouTube 字幕抓取失败时，之前会将空壳 fallback 内容送入 LLM，导致生成完全无关的内容。现在改为直接向用户展示明确错误信息（含失败原因），阻断无效生成。

3. **YouTube URL 解析增强**：新增支持 `youtube.com/shorts/`、`youtube.com/live/`、`youtube.com/v/` 等 URL 格式。

4. **YouTube 字幕回退策略优化**：重构字幕获取优先级 — 手动英文字幕 → 自动生成英文 → 任意英文 → 中文翻译英文 → 任意语言翻译英文，每级用精准的 `NoTranscriptFound` 异常判断而非笼统 `except Exception`。

### 本次使用 AI 技术
- **Streamlit on_click 回调模式**：利用 `st.button(on_click=callback, args=(...))` 在 text_input 控件渲染前写入 session_state，规避 Streamlit 的控件所有权检查
- **youtube-transcript-api 细粒度异常处理**：引入 `NoTranscriptFound`、`TranscriptsDisabled`、`VideoUnavailable` 等特定异常类，替代笼统的 `except Exception`，实现精准的降级回退

### 开发落地难点

1. **Streamlit session_state 所有权机制**：Streamlit 要求每个 widget key 只能由对应 widget 写入。quick topics 按钮在 text_input 之后渲染，导致跨控件写入冲突。解决方案是将 quick topics 提前到 text_input 之前，利用 `on_click` 回调在控件创建前完成写入。

2. **YouTube 字幕 API 的不稳定性**：`youtube-transcript-api` 实际使用的是 YouTube 的内部 API，时有被限流或变更的风险。解决思路是建立完整的优先级回退链，确保即使某个层级失败也有备选方案，并在所有层级都失败时给出有意义的用户提示而非静默降级。

### 解决方案与取舍
- ✅ 将 quick topics 移至输入框上方：技术上解决了 session_state 冲突，UI 上快捷入口更醒目
- ✅ 使用 `on_click` 回调而非直接修改 session_state：符合 Streamlit 推荐模式
- ✅ 阻断 fallback 内容进入 LLM：宁可失败可见，也不生成垃圾内容
- ❌ 不做 YouTube oEmbed/Data API v3 集成：需 API key，与项目「零外部依赖」原则冲突

### 产品复盘思考
这次修复揭示了一个重要的产品设计原则：**「优雅降级」不等于「静默失败」**。原代码在内容抓取失败时做了 fallback，但 fallback 的质量不足以支撑 LLM 生成有意义的脚本，结果就是「看起来没报错，但产出的是垃圾」。正确的做法是：当核心输入质量不达标时，应该明确告知用户并阻止后续流程，而不是硬着头皮继续跑完 pipeline。

另一个收获是关于 Streamlit 的 session_state 模型理解。Streamlit 的 widget key 体系本质上是一个「每个 key 有且只有一个 owner widget」的约束，这和 React 的受控组件理念有相似之处。理解这个模型后，调整控件顺序和使用回调函数就成为了自然的解决方案。

---

## v1.0.3 — YouTube 字幕 API 兼容修复 + 学习材料质量增强

### 迭代基础信息
- **更新时间**：2026-05-19
- **版本号**：v1.0.3
- **迭代类型**：修复 + 优化

### 本次改动核心功能

1. **YouTube 字幕 API 兼容修复**：`youtube-transcript-api` 新版本中 `list_transcripts` 类方法已移除，改为实例方法 `list()`。修复了 `AttributeError: type object 'YouTubeTranscriptApi' has no attribute 'list_transcripts'` 错误，YouTube 链接恢复正常识别和字幕抓取。

2. **学习材料 URL 可访问性验证**：MaterialSkill 新增 `_check_url()` 方法，对 LLM 生成的每个链接做 HEAD/GET 双重验证。HEAD 失败时自动降级为 GET stream 验证，确保推送给用户的链接都是真实可访问的。

3. **精准 5 篇推送 + 重试机制**：LLM 生成 8 篇候选 → URL 验证过滤 → 取前 5 篇有效链接。不足 5 篇时自动重试（最多 3 次），确保每次推送数量准确且质量可靠。

4. **material_curator.txt 提示词重构**：新增 approved sources 白名单（Anthropic/OpenAI/Google/Meta 官方文档、LangChain/HuggingFace/GitHub 开发平台、The Verge/TechCrunch 等 AI 媒体、Lenny's/Pragmatic Engineer 等产品社区），约束 LLM 只用稳定平台的真实 URL，杜绝幻觉链接。

5. **Quick Topics UI 优化**：快捷主题按钮从全宽改为 pill 胶囊样式（圆角边框、hover 琥珀色高亮），Generate 按钮单独定制主色样式。

### 本次使用 AI 技术
- **youtube-transcript-api 新版本 API 迁移**：类方法 `list_transcripts()` → 实例方法 `api.list()`，TranscriptList 过滤 API 保持不变
- **URL 验证双保险**：HEAD 请求优先（轻量），失败后回退 GET stream（兼容不支持 HEAD 的服务器）
- **生成-验证循环模式**：LLM 生成候选 → 程序化验证 → 不达标自动重试，将 LLM 的不可靠性和程序的可靠性结合

### 开发落地难点

1. **youtube-transcript-api 版本漂移**：该库不在 `requirements.txt` 中锁定版本，pip 安装时自动获取最新版，导致 API 签名变化后直接崩溃。根因是没有锁版本号。后续需在 `requirements.txt` 中锁定 `youtube-transcript-api>=0.x,<1.0` 或指定具体版本。

2. **LLM 幻觉 URL 问题**：之前 material_curator 生成的 URL 有相当比例是 LLM 编造的（格式正确但页面不存在）。解决思路是「源头约束 + 事后验证」双管齐下：prompt 层用 approved sources 白名单限制生成范围，代码层用 HTTP 请求验证每个链接。这种「LLM + 程序化验证」的混合模式是 AI 产品中处理幻觉的通用范式。

### 解决方案与取舍
- ✅ 使用 `YouTubeTranscriptApi().list(video_id)` 实例方法，保持 5 级回退逻辑不变
- ✅ 新增 URL 验证 + 重试机制：牺牲少量生成时间（+5-10秒），换取 100% 链接可访问
- ✅ 用 approved sources 白名单替代「URL 必须真实」的模糊约束：给 LLM 具体边界比抽象要求更有效
- ❌ 不做 youtube-transcript-api 版本锁定：当前问题已修复，版本锁定留到后续统一处理

### 产品复盘思考
这次 YouTube 修复再次验证了一个教训：**外部依赖的 API 变更是最常见的静默炸弹**。`youtube-transcript-api` 作为一个非官方库，API 稳定性没有保证。更稳健的做法是在 CI 中加入依赖 API 兼容性检查，或在 `requirements.txt` 中锁定版本。但对于个人项目，过度工程化的版本管理不值得 — 快速发现、快速修复的节奏更适合 Vibe Coding 阶段。

MaterialSkill 的 URL 验证改造体现了一个 AI PM 产品设计的核心原则：**不要期望 LLM 能做所有事，把「确定性」留给代码，把「创造性」留给 LLM**。URL 是否存在是确定性判断，用代码验证；资源选题和描述是创造性工作，交给 LLM。这个边界划分让产品质量从「有时候链接打不开」提升到「每个链接都可访问」。

---

## v1.0.4 — Tab 导航从 HTML href 重构为 st.pills 图标+文字融合式标签

### 迭代基础信息
- **更新时间**：2026-05-19
- **版本号**：v1.0.4
- **迭代类型**：优化

### 本次改动核心功能
将三页签导航从自定义 HTML `<a href="?tab=...">` 链接重构为 Streamlit 原生 `st.pills` 组件。旧方案每次切换触发浏览器完整页面刷新（白屏闪烁），新方案利用 `session_state` 实现零页面刷新的毫秒级切换。每个 tab 块内图标（emoji）+ 文字融为一体，无独立 icon 行、无勾选圆圈，视觉上干净统一。

### 本次使用 AI 技术
- **st.pills（Streamlit 1.36+）替代 HTML href 导航**：原生块状选择器，`selection_mode="single"` + `session_state` 做 tab 状态管理，天然无 radio 指示器
- **CSS 自定义 st.pills 样式**：匹配品牌温暖学术风格（圆角顶部边框 + 琥珀色选中态 + 去阴影）

### 开发落地难点
旧方案使用 URL query params（`?tab=speak`）+ `target="_self"` 触发浏览器整页刷新。这是 Streamlit 早期常见的多页签实现方式，但体验很差。中间尝试过 `st.radio` 方案，但 radio 带有勾选圆圈指示器且 icon 与文字分离为两个区域，不符合设计要求。最终采用 `st.pills`，图标和文字天然融合在同一个块内，无任何 extra UI 元素。

### 解决方案与取舍
- ✅ 用 st.pills + session_state 替代 URL params：切换零黑屏，图标文字融为一体
- ✅ 保持三页签视觉效果和品牌调性一致
- ❌ 不再支持通过 URL 参数直接跳转到指定 tab：接受这个 trade-off

### 产品复盘思考
同样的切换需求，经历了 HTML href（全页刷新）→ st.radio（有勾选圆点，icon 分离）→ st.pills（图标文字融合，无多余 UI）三次迭代。选组件时不仅要看功能，还要看它自带的默认 UI 是否符合设计预期 — st.pills 的默认形态恰巧就是我们要的「图标+文字融合式块状按钮」，零额外改造。

---

## v1.0.5 — YouTube 字幕抓取双引擎 + IP 风控绕过

### 迭代基础信息
- **更新时间**：2026-05-19
- **版本号**：v1.0.5
- **迭代类型**：修复 + 优化

### 本次改动核心功能

1. **yt-dlp 主力引擎**：替代 `youtube-transcript-api` 作为 YouTube 字幕抓取的第一方案。通过 Chrome 浏览器 cookies + HTTP 代理双重验证绕过 YouTube bot 检测，成功获取此前被 IP 风控拦截的视频字幕。

2. **双引擎回退机制**：yt-dlp（主力，cookies + proxy）→ youtube-transcript-api（备选，proxy only）→ 失败提示。任一引擎成功即返回结果，无需用户感知底层切换。

3. **VTT 字幕清洗**：yt-dlp 返回 WebVTT 格式字幕，自动清洗 HTML 标签、时间戳、元数据头，输出纯文本供 LLM 生成脚本。

### 本次使用 AI 技术
- **yt-dlp**：YouTube 视频信息提取库，支持 cookies-from-browser（Chrome）+ proxy 双重验证，绕过 YouTube 风控
- **VTT 格式解析**：正则清洗 WebVTT 字幕格式（标签、时间戳、头信息），转为纯文本
- **requests + proxy**：通过系统代理端口（4780）下载字幕文件

### 开发落地难点

1. **YouTube IP 风控升级**：`youtube-transcript-api` 即使配置了代理，部分视频仍被 YouTube 拦截（代理 IP 本身也可能被标记）。根因是 YouTube 对非浏览器流量的检测越来越严格。yt-dlp 的 cookies-from-browser 功能让请求看起来像是从真实浏览器发出的，绕过了大部分检测。

2. **Cookies 依赖**：yt-dlp 的 `cookiesfrombrowser` 依赖用户 Chrome 保持登录 YouTube 状态。这意味着个人部署场景下 cookie 始终有效，但云端部署（Streamlit Cloud）会遇到无浏览器可用的问题。

3. **字幕格式差异**：yt-dlp 返回 WebVTT 格式（含时间戳和样式标签），youtube-transcript-api 返回结构化 JSON。需要针对两种格式分别做清洗。

### 解决方案与取舍
- ✅ yt-dlp 作为主引擎：通过 cookies + proxy 突破 IP 风控，成功率远超 youtube-transcript-api
- ✅ 保留 youtube-transcript-api 作为备选：某些视频 yt-dlp 也可能失败，双引擎确保最大覆盖率
- ✅ 统一输出格式：两种引擎清洗后输出相同的纯文本
- ❌ Streamlit Cloud 部署时 yt-dlp cookies 不可用，将自动回退到 youtube-transcript-api + proxy：接受这个限制

### 产品复盘思考
YouTube 内容抓取是产品的核心依赖，但它的反爬策略在持续升级。从最初的直接 API 调用，到加代理，再到 cookies + 代理双验证，这是一场持续的猫鼠游戏。产品层面的思考是：**外部依赖的不确定性是 AI 产品的常态，与其追求 100% 覆盖率，不如在失败时给出清晰指引** — 让用户知道为什么失败、可以怎么尝试（如直接输入主题关键词绕过抓取）。

---

## v1.0.6 — Quick Topics 动态生成 + AI Radar 来源扩展 + 近期内容推荐

### 迭代基础信息
- **更新时间**：2026-05-20
- **版本号**：v1.0.6
- **迭代类型**：优化

### 本次改动核心功能

1. **Quick Topics 动态生成**：从固定 6 个枚举字符串改为每次新会话由 LLM 动态生成。每批 6 个话题，严格 5-10 词长度，跨英语学习/健康/AI/时尚/新闻/旅行/美食等 12 个领域随机轮换。`session_state` 缓存，同一次访问不重复调用 LLM。

2. **AI Radar 来源大幅扩展**：从仅限 approved sources 白名单（约 25 个网站）扩展为：原有 AI 官方文档/开发平台/科技媒体 + 新增 YouTube（AI 频道如 Two Minute Papers、Fireship 等）+ Bilibili + Substack + 个人博客。覆盖面从纯文本扩展到视频、播客、Newsletter 等多媒介。

3. **近期内容强约束**：prompt 新增「所有内容必须在 30 天内」硬性要求，筛选逻辑从「知名平台即可」改为「近期热点 + 技术学习」双维度。早间侧重热点新闻 + 实用工具，晚间侧重技术深读 + 行业分析。

4. **数量精准化**：prompt 从「生成 8 篇候选 → 验证取 5 篇」简化为「直接生成 5 篇 + 验证」，UI 文案和代码同步。

### 本次使用 AI 技术
- **LLM 动态话题生成**：轻量 prompt（~150 tokens），每次生成 6 个跨领域话题，温度默认 0.7 保证多样性
- **Prompt Engineering**：material_curator.txt 新增时效性约束（30 天内）+ 双维度混合（热点 : 技术 ≈ 1:1）

### 开发落地难点
Quick Topics 的「何时重新生成」是一个产品决策点：每次 rerun 生成会导致 LLM 调用过频（Streamlit 每次交互都 rerun），永久缓存则失去动态意义。最终选择 `session_state` 缓存 — 会话内不重复生成，刷新页面或新窗口访问时重新生成。这对应一个产品判断：用户在一次访问中不会期望话题变化，但每次来网站看到新话题是价值点。

### 解决方案与取舍
- ✅ Quick Topics 动态化：从 0 调用成本（硬编码）变为每次访问 1 次轻量 LLM 调用（~150 tokens），换来话题新鲜感
- ✅ AI Radar 来源扩展：从封闭白名单到开放平台 + 多媒体，时效性从「无要求」到「30 天内」
- ❌ 不做个性化推荐（基于用户历史生成脚本的题材分析）：当前阶段过度工程化
- ❌ 不做话题去重：两次访问可能看到相似话题，但概率低，暂不处理

### 产品复盘思考
Quick Topics 从「死数据」变「活数据」这个改动虽小，但对产品体验影响明显 — 用户每次打开网页都能看到不同的话题推荐，产生「系统在持续运营」的感知。这也是 AI 产品与传统产品的差异点：**AI 让「每次不同」成为可能，且边际成本几乎为零**。

AI Radar 的来源扩展和时效性约束，本质上是将推荐质量从「靠谱」提升到「前沿」。之前只要 URL 打得开就算通过，现在要求内容必须新鲜且热门，这对 AI PM 用户的职业发展价值更大。

---

## v1.0.7 — AI Radar 全量展示 + session_description 数字自动修正

### 迭代基础信息
- **更新时间**：2026-05-21
- **版本号**：v1.0.7
- **迭代类型**：修复 + 优化

### 本次改动核心功能

1. **AI Radar 全量展示重构**：从「最新推送横幅 + 历史展开面板」两级结构改为「全部推送按时间倒序完整展示」，每期内容均为可见卡片，不再隐藏在 expander 中。用户无需点击展开即可浏览所有日期的推送内容。

2. **session_description 数字自动修正**：新增正则自动修正逻辑（`re.sub(r'\d+[个篇条项]', f'{target_count}个', session_desc)`），无论 LLM 在描述文案中写了几个资源（"8个"、"8篇"等），都会被自动替换为正确的实际数量。同时修正了 prompt 模板和已有的错误数据记录。

### 本次使用 AI 技术
- **Regex 后处理守卫**：在 LLM 输出后增加自动化的数字一致性检查，确保「文案描述的数字」等于「实际推送数量」。这是一个典型的「LLM 输出可信度增强」模式 — 不依赖 LLM 遵守约束，而是在代码层兜底。
- **Streamlit 全量渲染**：将单次推送的卡片渲染逻辑提取为 `_render_material_card()` 辅助函数，对所有历史记录复用循环渲染，保证视觉一致性。

### 开发落地难点

1. **LLM 不遵守数字约束**：尽管 prompt 明确要求「exactly 5 resources」，但 LLM 在生成 session_description 时仍会自行写入"8"（可能是模型预训练数据中「推荐内容」常与"8"搭配的统计偏差）。单独增强 prompt 约束无法根除问题，必须在代码层加正则修正作为质量守卫。

2. **全量展示 vs 信息过载的平衡**：将全部推送平铺展示后，页面会变长。当前数据量（7 条推送）完全可控，但如果数据累积到 50+ 条时需要引入「折叠旧推送」或分页机制。这是产品设计中的「当前需求 vs 未来扩展」权衡 — v1.0.7 先满足当前需求，不过度工程化。

### 解决方案与取舍
- ✅ 全量展示替代两级结构：从信息架构上简化，所有推送同等重要，不需要区分「最新」和「历史」
- ✅ 正则修正做兜底而非唯一方案：prompt 修正 + 代码守卫双保险
- ✅ 提取 `_render_material_card()` 复用：消除历史推送和最新推送的重复渲染代码
- ❌ 不做分页/无限滚动：当前数据量不需要，后续数据积累到阈值时再引入

### 产品复盘思考
这次修复触及了 AI 产品中的一个常见陷阱：**prompt 约束是有上限的**。LLM 不是数据库，不能用 `CHECK (count = 5)` 约束 — 即使 prompt 说「output exactly 5」，它在写描述文案时仍可能随手写个"8"。正确的做法是**把结构化约束放在代码层，把内容创作放在 prompt 层**。这是「LLM + 程序化守卫」混合架构的又一次验证。

另一层思考是关于信息架构：之前的「最新推送横幅 + 历史展开」是一种「时效性优先」的设计，隐含假设是用户只关心最新一条。但实际上，AI Radar 的价值在于「持续积累的内容库」，用户可能会回头看前几天的推荐。全量展示让所有推送平等可见，更符合这个产品的实际使用场景。

---

## v1.0.8 — YouTube URL 识别全面修复（3 类场景）

### 迭代基础信息
- **更新时间**：2026-05-23
- **版本号**：v1.0.8
- **迭代类型**：修复

### 本次改动核心功能

修复 YouTube URL 识别的 3 类失败场景，涵盖 8 种具体 URL 格式：

1. **`v` 参数非首位查询参数时 ID 提取失败**（4 种）：当 YouTube 分享链接的查询参数顺序为 `?si=xxx&v=VIDEO_ID`（或 `app`、`feature`、`t` 等其他参数先于 `v` 出现）时，旧版正则 `watch\?v=` 要求 `v` 紧跟在 `?` 之后，导致无法提取视频 ID。改用 `urlparse` + `parse_qs` 标准库解析查询字符串，从参数字典中按 key 取值，彻底消除顺序依赖。

2. **无协议前缀的裸域名 URL 不被识别为链接**（3 种）：用户从聊天软件、短信等渠道复制的 YouTube 链接常缺失 `https://` 前缀（如 `youtube.com/watch?v=xxx`、`youtu.be/xxx`）。旧版 `_is_url` 仅匹配 `http(s)://` 开头的字符串，导致这些链接被当作纯文本主题处理。新版增加裸域名识别（匹配 `域名.顶级域/路径` 模式），并在识别后自动补全 `https://` 前缀。

3. **大写域名不匹配平台检测**（1 种）：正则匹配默认大小写敏感，`YOUTUBE.COM` 无法匹配 `youtube\.com`。在所有平台检测和 ID 提取的正则中增加 `re.IGNORECASE` 标志。

### 本次使用 AI 技术
- **标准库 `urlparse` + `parse_qs` 替代手写正则**：用经过充分测试的标准库解析 URL，比手写正则覆盖更多边缘格式（fragment、编码字符等）
- **防御性兜底逻辑**：在 `_extract_youtube_id` 末尾增加通用 `parse_qs` 兜底，即使 URL 路径不匹配任何已知模式，只要查询参数中有 `v` 就能正确提取

### 开发落地难点

1. **正则表达式维护负担**：旧版手写 6 条正则分别匹配不同 URL 格式，每次 YouTube 分享格式微调（如新增 `si` 参数）都可能导致新的不兼容。根因是正则本质上是「模式匹配」而非「结构化解析」—— 对于 URL 这种有标准语法的格式，结构化解析是正确方案。

2. **`_is_url` 放宽后的误判风险**：增加裸域名识别后，理论上「hello.world/today」这类短语可能被误判为 URL。但实际上用户输入大多是明确的链接或明确的话题短语，误判概率极低。即使误判，后续内容抓取失败也会给出明确提示，不会静默生成垃圾内容。

3. **URL 解析库与 `youtu.be` 短链接的交互**：`urlparse` 对 `youtu.be/VIDEO_ID`（无协议）的解析行为与有协议的 URL 不同 — 无协议时会将整个字符串视为 path。通过在 `_extract_youtube_id` 入口处先做 host 判断（基于 `netloc` + fallback 正则），确保两种格式都正确处理。

### 解决方案与取舍
- ✅ 用 `urlparse` + `parse_qs` 替代手写正则提取 `v` 参数：从「匹配特定模式」升级为「解析查询语义」
- ✅ 在所有正则匹配中增加 `re.IGNORECASE`：域名大小写在 DNS 层面就是不敏感的
- ✅ 增加 `_normalize_url` 自动补全协议前缀：对用户透明，输入体验无感知
- ✅ 保留末尾 `parse_qs` 兜底：即使未来出现新的 YouTube URL 格式，只要 query string 里有 `v` 就能兜住
- ❌ 不做 URL 有效性预验证（HEAD 请求检查视频是否存在）：会增加延迟，且抓取引擎自身的错误处理已足够

### 产品复盘思考
这次修复揭示了一个通用规律：**手写正则做 URL 解析，覆盖率永远追不上格式变化**。YouTube 的分享 URL 格式在过去几年里经历了多次调整（增加 `si` 参数、新增 `shorts/` 路径、`youtu.be` 短链接等），每次调整都会打破依赖固定格式的正则。正确的做法是将 URL 视为有标准语法的结构化数据，用解析器而非模式匹配来处理。

另一个维度是关于「URL 输入容错性」的产品设计：用户从不同渠道复制链接时，粘贴的内容格式不一（有/无协议、有/无 www、大小写混用）。作为产品，应该尽可能宽容地接受输入，而不是要求用户按照特定格式粘贴。这也是「用户输入容错性」作为 AI 产品质量维度的一个体现。

---

## v1.0.7 — AI Radar 自动 Git 同步

### 迭代基础信息
- **更新时间**：2026-05-28
- **版本号**：v1.0.7
- **迭代类型**：新增

### 本次改动核心功能
1. **Material push 自动 Git 同步**：每次执行 `material push` 策展推送后，自动将 `data/` 目录变更 `git add` + `git commit` + `git push`，确保本地推送 = 线上同步。解决了之前「数据天天推、线上只到 5/19」的数据断层问题。

### 本次使用 AI 技术
- 无新增 AI 能力，纯工程自动化改进

### 开发落地难点
1. **数据断层根因分析**：本地 data/ 目录从 5/18 到 5/28 每天都有推送记录（共 27 个未提交文件），但从未被 commit 到 git。Streamlit Cloud 从 git 拉取部署，只看到最早提交的 4 条数据。根因是 material push 只管生成本地数据，不管同步到远程仓库。

### 解决方案与取舍
- ✅ 在 `_cmd_push` 末尾追加 `_git_sync()`，save 后自动 commit + push
- ✅ failed gracefully：git 同步失败时打印 warning，不影响推送主流程
- ❌ 不做 CI/CD 侧定时调度：保持简单，push 即同步，不引入额外定时器

### 产品复盘思考
这次问题的本质是「离线生成 + 在线展示」架构下的数据同步断层。数据生成在本地完成，但展示依赖远程 git 仓库。如果没有自动同步机制，数据必然越积越多但永远到不了线上。解决思路是将「同步」嵌入到「生成」流程中，让每次生成都自动触达线上，而不是依赖人工记得去 commit。这也是 AI Agent 产品中「最后一步自动化」的典型案例——功能链路已经跑通，但缺少把结果送到用户面前的最后一步。

---

## v1.0.8 — YouTube 抓取代理优先策略

### 迭代基础信息
- **更新时间**：2026-05-29
- **版本号**：v1.0.8
- **迭代类型**：修复

### 本次改动核心功能
1. **YouTube 抓取代理优先**：`_try_request` 和 youtube-transcript-api 原为先直连后代理，国内直连 YouTube 被 GFW 丢包，urllib3 默认 3 次重试 × 12s 超时 = 浪费 30-40 秒等待。改为代理优先，直连作为 5s 快速兜底。

### 本次使用 AI 技术
- 无新增 AI 能力，网络层策略优化

### 开发落地难点
1. **问题复现困难**：代理 clashr-da 实际在 4780 端口正常运行，命令行测试代理可用，但用户操作时偶发失败。根因是直连 GFW 丢包 + urllib3 重试机制叠加，导致用户在 Streamlit UI 看到长时间 spinner 后报错，实际可能是总超时而非代理不可用。

### 解决方案与取舍
- ✅ 代理优先：`proxies=[proxy_cfg, None]` 替代原来的 `[None, proxy_cfg]`
- ✅ 兜底直连缩短超时：直连降为 5s，代理保持 12s
- ❌ 不做智能网络检测：保持简单，代理优先对国内用户最优

### 产品复盘思考
网络策略的设计要考虑目标用户的实际网络环境。国内用户访问 YouTube 时，直连几乎是注定失败的，先直连相当于每次都浪费 30 秒在无效等待上。对于海外用户（如 Streamlit Cloud 部署环境），本地代理不存在，直连反而是唯一通路。所以代理优先 + 直连兜底的策略同时覆盖了两种场景——代理可用时秒连，代理不可用时快速回退直连。
